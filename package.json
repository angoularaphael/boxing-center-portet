{
  "name": "boxing-center-portet",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Site vitrine immersif — Boxing Center Portet-sur-Garonne",
  "scripts": {
    "_note_prepare": "npm install branche le crochet qui retire les signatures d'outil des messages de commit (voir .githooks/commit-msg)",
    "prepare": "git config core.hooksPath .githooks",
    "dev": "vite",
    "build": "node scripts/optimize-images.mjs && node scripts/sitemap-images.mjs && node scripts/generate-llms.mjs && vite build && node scripts/cuire-md.mjs && node scripts/garde-auteurs.mjs",
    "generate:llms": "node scripts/generate-llms.mjs",
    "preview": "vite preview --port 4173",
    "generate:favicons": "node scripts/sync-boxplus-favicons.mjs",
    "generate:google-favicons": "node scripts/generate-google-favicons.mjs",
    "generate:og": "node scripts/generate-favicons.mjs",
    "optimize:images": "node scripts/optimize-images.mjs"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "sharp": "^0.35.1",
    "sortablejs": "^1.15.7",
    "typescript": "^5.6.0",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "cloudinary": "^2.5.0",
    "gsap": "^3.12.5",
    "lenis": "^1.1.18",
    "three": "^0.169.0"
  }
}
